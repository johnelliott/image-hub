extends layout.pug
block content
  h2 🖼 #{list.length}  
  ul
    each val in list
      ul
        - var href = val.href;
        - var src = val.b64i;
        a(href= href)
          img(src=`data:image/jpg;base64,${src}`)
